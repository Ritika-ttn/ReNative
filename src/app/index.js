import React from 'react';
import {View, Text} from 'react-native';
import {Api} from 'renative';
import ReNativeEx from '../components/ReNativeEx';
const App = () => (
  <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
    <ReNativeEx />
  </View>
);

export default App;
